---
const {data,index}= Astro.props
---

<section class=" max-w-7xl w-full mx-auto">
    <div class={"flex justify-between " + " " +(index%2 == 0 ? 'flex-row':'flex-row-reverse')}>
        <div>
            Tout voir
        </div>
        <div>
            {data.attributes.title}
        </div>
    </div>
</section>